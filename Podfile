# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'weatherClientSwift' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'SwiftLint'
  pod 'KeychainSwift', '~> 11.0'
  # Pods for weatherClientSwift

  target 'weatherClientSwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'weatherClientSwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
